# MySQL Database Configuration
# Actual credentials for local MySQL server
# OPTIMIZED for multi-developer environment to prevent "Too many connections"

# MySQL Server Connection
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=plat_detection

# Connection Pool Settings (OPTIMIZED - 40% reduction to prevent exhaustion)
MYSQL_POOL_SIZE=3
MYSQL_MAX_OVERFLOW=10
MYSQL_POOL_TIMEOUT=30

# Connection Timeout Settings (AUTO-CLEANUP enabled)
# Idle connections auto-closed after 5 minutes (300 seconds)
# Health check runs every 60 seconds to remove broken connections
MYSQL_MAX_IDLE_TIME=300
MYSQL_HEALTH_CHECK_INTERVAL=60

# Feature Flags
USE_MYSQL_DATABASE=True
ENABLE_ACCESS_CONTROL=True
LOG_DENIED_ACCESS=True
AUTO_UPDATE_VEHICLE_STATUS=True

# Dual Mode Settings (SQLite + MySQL)
ENABLE_SQLITE_LOGGING=True
ENABLE_MYSQL_ACCESS_CONTROL=True
